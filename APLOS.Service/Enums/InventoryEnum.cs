namespace Library.Service.Enums
{
    public enum Approval
    {
        Select,
        Hold,
        Reject,
        Checked
    }
    public enum Checked
    {
        Select,
        Hold,
        Reject,
        Approved
    }
}