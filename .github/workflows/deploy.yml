name: Deploy PratibhaPOP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Restore NuGet Packages
        run: nuget restore APLOS.New.sln

      - name: Copy Aplos.Service.Extension DLL
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path "$env:GITHUB_WORKSPACE\Aplos.Service.Extension\bin\Debug"

          Copy-Item `
            "F:\aPOP\Pratibha\IIS\pratibhaPOP\bin\Aplos.Service.Extension.dll" `
            "$env:GITHUB_WORKSPACE\Aplos.Service.Extension\bin\Debug\" `
            -Force

      - name: Build Solution
        run: |
          msbuild APLOS.New.sln /p:Configuration=Release

      - name: Find Aplos DLL
        shell: powershell
        run: |
          Write-Host "===== Aplos.dll Locations ====="
          Get-ChildItem "$env:GITHUB_WORKSPACE" -Recurse -Filter Aplos.dll |
          Select-Object FullName

      - name: Show Release Folders
        shell: powershell
        run: |
          Write-Host "===== Release Folders ====="
          Get-ChildItem "$env:GITHUB_WORKSPACE" -Recurse -Directory |
          Where-Object { $_.FullName -match "bin\\Release" } |
          Select-Object FullName

      - name: Stop IIS App Pool
        shell: powershell
        run: |
          Import-Module WebAdministration
          Stop-WebAppPool -Name "pratibhapop"

      - name: Copy DLL Files
        shell: powershell
        continue-on-error: true
        run: |
          robocopy `
            "$env:GITHUB_WORKSPACE\APLOS\bin\Release" `
            "F:\aPOP\Pratibha\IIS\pratibhaPOP\bin" `
            *.dll *.pdb /E /R:1 /W:1

          if ($LASTEXITCODE -le 3) {
            exit 0
          } else {
            exit $LASTEXITCODE
          }

      - name: Start IIS App Pool
        if: always()
        shell: powershell
        run: |
          Import-Module WebAdministration
          Start-WebAppPool -Name "pratibhapop"
