<togglable name="System Admin Locked"></togglable>
<div class="form-elements">
    <div class="col-xs-12 col-sm-6 col-md-6 form-horizontal">
        <div class="form-group">
            <label class="col-sm-4 control-label">System Admin</label>
            <div class="col-sm-8">
                <label class="col-sm-8 control-label">:  {{sysAdmin.UserId}}</label>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-4 control-label">Full Name</label>
            <div class="col-sm-8">
                <label class="col-sm-8 control-label">:  {{sysAdmin.FullName}}</label>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-4 control-label">Last Lock Time</label>
            <div class="col-sm-8">
                <label class="col-sm-8 control-label">:  {{sysAdmin.UserLockedDate.replace('/Date(','').replace(')/','') | date:'dd-MMM-yyyy HH:mm:ss'}}</label>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-4 control-label">Total SysUser Lock Time</label>
            <div class="col-sm-8">
                <label class="col-sm-8 control-label">:  {{sysAdmin.TotalUserLockTime}}</label>
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-4 control-label">Total Password Fail Count</label>
            <div class="col-sm-8">
                <label class="col-sm-8 control-label">:  {{sysAdmin.PasswordFailCount}}</label>
            </div>
        </div>
    </div>
    <div class="common-btn">
        <ul class="list-inline text-right">
            <li>
                <button ng-click="passValue(sysAdmin.Id, $index)" data-toggle="modal" data-target="#archivePopUp" class="btn">
                    <i class="glyphicon glyphicon-lock"></i> {{Action}}
                </button>
            </li>
            <li> <a href="#user-lock-log/{{sysAdmin.Id}}" class="btn"> Log</a></li>
        </ul>
    </div>
</div>
<div class="row text-right">
    <div class="col-xs-12 col-sm-12 col-sm-12">
        <div class="search-form">
            <div class="row">
                <div class="col-xs-12 col-sm-3 col-md-3">
                    <div class="select-style">
                        <select ng-model="$root.parameters.searchBy">
                            <option ng-repeat="option in searchByList" value="{{option.value}}">{{option.name}}</option>
                        </select>
                    </div>
                </div>
                <div class="col-xs-12 col-sm-7 col-md-7">
                    <input type="text" class="form-control" placeholder="Search" ng-model="$root.parameters.search" ng-enter="getData()">
                </div>
                <div class="col-xs-12 col-sm-2 col-md-2">
                    <button type="button" ng-click="getData()" class="btn"><i class="glyphicon glyphicon-search"></i> Search</button>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="table-responsive modal-table">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Username</th>
                <th>Full Name</th>
                <th>Auth Token Token</th>
                <th>Company Group</th>
                <th>System Admin Locked</th>
                <th>System Admin Locked Date</th>
            </tr>
        </thead>
        <tbody>
            <tr dir-paginate="x in sysAdmins |itemsPerPage:$root.pageSize" total-items="$root.total_count" ng-click="Get(x.Id, $index);">
                <td>{{x.UserId}}</td>
                <td>{{x.FullName}}</td>
                <td>{{x.AuthToken}}</td>
                <td>{{x.CompanyGroupName}}</td>
                <td>{{x.UserLocked?'Locked':'UnLocked'}}</td>
                <td>{{x.UserLockedDate.replace('/Date(','').replace(')/','') | date:'dd-MMM-yyyy HH:mm:ss'}}</td>
            </tr>
        </tbody>
    </table>
    <dir-pagination-controls max-size="10" direction-links="true" boundary-links="true" on-page-change="getData(newPageNumber)"></dir-pagination-controls>
</div>
<confirm-archive message="Are you sure want to unlock this" body="{{sysAdmin.UserId}}" data-ng-click-right-button="userUnLock()"></confirm-archive>